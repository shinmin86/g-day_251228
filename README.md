# Static Invitation (GitHub Pages)
1) Upload all files to a GitHub repository (root).
2) Settings → Pages → Deploy from a branch → main / (root).
3) Replace images in `assets/`:
   - hero.jpg
   - gallery1.jpg ~ gallery9.jpg
4) Edit texts in `index.html` and phone numbers in `app.js`.
